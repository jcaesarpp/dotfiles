general {
        colors = true
        interval = 5
}

order += "battery all"
order += "tztime local"

tztime local {
        format = "%b %d, %a %H:%M"
}

battery all {
        format = "%status %percentage %remaining"
}

