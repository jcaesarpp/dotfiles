[user]
	name = Julio C. Pompa Puente
	email = julio.c.pompa.puente@gmail.com

[core]
	excludesfile = ~/.dotfiles/gitignore
	hooksPath = ~/.dotfiles/git/hooks
	compression = 0
	packedGitLimit = 512m 
	packedGitWindowSize = 512m

[credential]
	helper = store --file ~/.private/git/git-credentials

[push]
	followTags = true

[alias]
	coa = !git add -A && git commit -am

[init]
	defaultBranch = main

[pack] 
	deltaCacheSize = 2047m 
	packSizeLimit = 2047m 
	windowMemory = 2047m
